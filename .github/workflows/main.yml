# main.yml
name: Test Action

on:
  push:
    branches:
      - main  # Trigger the action on push to the `main` branch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23f1002643@ds.study.iitm.ac.in
        run: echo "Hello, world!"

